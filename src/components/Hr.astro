---
export interface Props {
  noPadding?: boolean;
}

const { noPadding = true} = Astro.props;
---


<hr class={`z-[9999] origin-left w-full  border-skin-line mx-auto   ${noPadding ? "px-0" : "px-4"}   `} aria-hidden="true" />

